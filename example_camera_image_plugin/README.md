# example_camera_image_plugin

An example Flutter plugin project, that uses the 'camera_image_method_channel' plugin.

Renders a video with the given frames, with an entrypoint for modifying the pixel buffers.